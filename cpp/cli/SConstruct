# SConstruct file for building cli.cpp using SCons

from SCons.Script import SConscript, VariantDir

# Use 'build' directory for all build artifacts, with parent directory as source
VariantDir('build', '.', duplicate=0)

# Import the SConscript from the build directory
SConscript('build/SConscript')
